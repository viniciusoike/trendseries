% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index_series.R
\name{index_series}
\alias{index_series}
\title{Index one or more series}
\usage{
index_series(
  data,
  date_col = "date",
  value_col = "value",
  group_cols = NULL,
  base_period = NULL,
  base_value = 100,
  na_rm = FALSE,
  suffix = NULL,
  .quiet = FALSE
)
}
\arguments{
\item{data}{A non-empty \code{data.frame}, \code{tibble}, or \code{data.table}.}

\item{date_col}{Name of the \code{Date} column. Defaults to \code{"date"}.}

\item{value_col}{Non-empty character vector naming numeric value columns.}

\item{group_cols}{Optional character vector naming grouping columns. Each
group receives its own reference value.}

\item{base_period}{\code{NULL} to use the earliest non-missing observation; one
or two four-digit integer years; or one or two \code{Date} values. Two values
define an inclusive range and may be supplied in either order. A single
date selects the calendar period containing it at the detected frequency.}

\item{base_value}{Finite positive number assigned to the reference. Defaults
to 100.}

\item{na_rm}{Whether to remove missing values when averaging an explicit
base period. With the default \code{FALSE}, a missing base observation raises
an error instead of blanking the entire indexed series. This argument has
no effect when \code{base_period = NULL}.}

\item{suffix}{Optional non-missing character suffix for generated names.}

\item{.quiet}{If \code{TRUE}, suppress frequency-detection messages. Warnings
about incomplete base periods are never suppressed.}
}
\value{
A tibble containing the original columns, in their original order,
followed by \verb{index_\{value_col\}} columns (and \verb{_\{suffix\}} when supplied).
Unlike \code{\link[=augment_trends]{augment_trends()}} and \code{\link[=augment_rolling]{augment_rolling()}}, rows remain in their
input order.
}
\description{
Rescale numeric series relative to their earliest observed value or to the
arithmetic mean over a selected base period.
}
\details{
When \code{base_period} is supplied, dates are matched at the detected calendar
frequency. Thus, for monthly data, \code{as.Date("2019-01-01")} also matches an
observation dated at month end. Weekly and daily series use exact interval
containment. A partly observed base interval produces a warning.
}
\examples{
vehicles |>
  index_series(value_col = "production")

retail_volume |>
  index_series(group_cols = "name_series", base_period = 2019)

}
\seealso{
\code{\link[=augment_trends]{augment_trends()}} for trend estimation and \code{\link[=augment_rolling]{augment_rolling()}} for
rolling and year-to-date aggregations.
}
