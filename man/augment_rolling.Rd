% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/augment_rolling.R
\name{augment_rolling}
\alias{augment_rolling}
\title{Add rolling aggregation columns to a data frame}
\usage{
augment_rolling(
  data,
  date_col = "date",
  value_col = "value",
  group_cols = NULL,
  stats = "sum",
  window = NULL,
  frequency = NULL,
  align = "right",
  percent = FALSE,
  na_rm = FALSE,
  suffix = NULL,
  .quiet = FALSE
)
}
\arguments{
\item{data}{A \code{data.frame}, \code{tibble}, or \code{data.table} containing the time
series data.}

\item{date_col}{Name of the date column. Defaults to \code{"date"}. Must be of
class \code{Date}.}

\item{value_col}{Name of the value column(s). Defaults to \code{"value"}. Must be
\code{numeric}. A character vector of length > 1 is accepted; aggregations are
computed for each column and named \verb{roll_\{stat\}_\{window\}_\{col\}}.}

\item{group_cols}{Optional grouping variables for multiple time series. Can
be a character vector of column names.}

\item{stats}{Character vector of rolling statistics. Options: \code{"sum"}
(rolling total of flows), \code{"chain"} (compound accumulation of rates,
\code{prod(1 + r) - 1}), \code{"mean"}, \code{"sd"}, \code{"min"}, \code{"max"}. Default is
\code{"sum"}.}

\item{window}{Window length in periods. If \code{NULL}, defaults to the detected
frequency (12 for monthly, 4 for quarterly). A numeric vector adds one
column per window value. Alternatively, the string \code{"ytd"} computes an
expanding year-to-date accumulation that resets each January (or Q1).
Numeric windows and \code{"ytd"} cannot be mixed in one call.}

\item{frequency}{The frequency of the series. Supports 4 (quarterly) or 12
(monthly). Auto-detected if not specified.}

\item{align}{Alignment of the window relative to the output position:
\code{"right"} (default), \code{"center"}, or \code{"left"}. Ignored when \code{window = "ytd"}.
An even window has no exact centre; see \code{\link[=roll_series]{roll_series()}} for how each
statistic handles that.}

\item{percent}{Only used by \code{stats = "chain"}. If \code{FALSE} (default), rates
are assumed to be decimals (0.005 for 0.5\%). If \code{TRUE}, rates are assumed
to be percentages (0.5 for 0.5\%) and the result is returned in percent.}

\item{na_rm}{If \code{TRUE}, missing values are ignored within each window. The
default \code{FALSE} propagates \code{NA}, so an incomplete window yields \code{NA}. A
window holding no observed values yields \code{NA} either way, as does a
window holding one value for \code{"sd"}.}

\item{suffix}{Optional suffix appended to the generated column names.}

\item{.quiet}{If \code{TRUE}, suppress informational messages.}
}
\value{
A tibble with the original data plus rolling columns named
\verb{roll_\{stat\}_\{window\}} (e.g. \code{roll_sum_12}, \code{roll_chain_ytd}), with
\verb{_\{suffix\}} appended when \code{suffix} is supplied. Rows come back in the
order they were supplied in.
}
\description{
Pipe-friendly companion to \code{\link[=augment_trends]{augment_trends()}} for rolling and year-to-date
aggregations: 12-month accumulated totals, compounded rates of change,
rolling volatility, and so on. Columns are prefixed \code{roll_} rather than
\code{trend_}, because these are aggregations of the series and not estimates of
its trend.
}
\details{
Use \code{"sum"} for flows measured in levels and \code{"chain"} for series that are
already rates of change. Summing monthly inflation rates approximates the
12-month accumulation but is not equal to it; \code{"chain"} compounds them
correctly. See \code{\link[=roll_series]{roll_series()}} for the underlying computation.

\code{"mean"} overlaps with the simple moving average available through
\code{augment_trends(methods = "ma")}. The two differ in defaults rather than in
substance: rolling aggregations default to right alignment, while the moving
average trend defaults to centred alignment. Given the same window and
alignment they agree, including the 2xN correction for even centred
windows.

Rows whose value is \code{NA} are kept in place, so window positions stay aligned
with the calendar; \code{na_rm} then decides whether such a window yields \code{NA} or
is computed from the observations that are present. Unlike
\code{\link[=augment_trends]{augment_trends()}}, which rejects gaps inside the observed range, a rolling
window has well-defined local behaviour for a gap, so these functions accept
one. A period that is absent
from the data altogether cannot be positioned, so it raises an error rather
than shifting later observations — add the missing rows with an \code{NA} value
first.
}
\examples{
# 12-month accumulated vehicle production
vehicles |> augment_rolling(value_col = "production", window = 12)

# Several windows at once
vehicles |>
  tail(60) |>
  augment_rolling(value_col = "production", window = c(3, 6, 12))

# Rolling mean and volatility side by side
ibcbr |>
  augment_rolling(value_col = "index", stats = c("mean", "sd"), window = 12)

# Year-to-date accumulation, resetting each January
vehicles |> augment_rolling(value_col = "production", window = "ytd")

# Grouped series
retail_volume |>
  augment_rolling(group_cols = "name_series", window = 12)

}
\seealso{
\code{\link[=roll_series]{roll_series()}} for the time series interface, \code{\link[=augment_trends]{augment_trends()}}
for trend estimation.
}
