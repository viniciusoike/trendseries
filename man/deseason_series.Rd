% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deseason_series.R
\name{deseason_series}
\alias{deseason_series}
\title{Seasonally adjust (deseason) a time series}
\usage{
deseason_series(
  data,
  date_col = "date",
  value_col = "value",
  group_cols = NULL,
  methods = "stl",
  transform = "none",
  frequency = NULL,
  components = FALSE,
  params = list(),
  .quiet = FALSE
)
}
\arguments{
\item{data}{A \code{data.frame}, \code{tibble}, or \code{data.table} containing the time series data.}

\item{date_col}{Name of the date column. Defaults to \code{"date"}. Must be of class \code{Date}.}

\item{value_col}{Name of the value column. Defaults to \code{"value"}. Must be numeric.}

\item{group_cols}{Optional grouping variables for multiple time series.
A character vector of column names. When provided, decomposition is applied
independently to each group.}

\item{methods}{Seasonal-adjustment method(s). One or more of \code{"stl"} (default) or
\code{"seats"}. When both are supplied, each contributes its own
\verb{seasadj_\{method\}} column (and component columns when \code{components = TRUE})
so the adjustments can be compared side by side.
\itemize{
\item \code{"stl"}: Seasonal-Trend decomposition via Loess (\code{stats::stl()}).
\item \code{"seats"}: X-13ARIMA-SEATS decomposition (requires the \strong{\code{seasonal}}
package; see \code{\link[=decompose_series]{decompose_series()}} for details).
}}

\item{transform}{Transformation applied to the series before decomposition.
One of \code{"none"} (default, additive decomposition) or \code{"log"}. With
\code{"log"}, the series is log-transformed, decomposed additively, and the
components are exponentiated back, yielding a \emph{multiplicative} decomposition.}

\item{frequency}{The frequency of the series. Supports 4 (quarterly) or 12
(monthly). Will be auto-detected if not specified. All methods require
\code{frequency > 1}.}

\item{components}{If \code{FALSE} (default), only the seasonally adjusted
\verb{seasadj_\{method\}} column is added. If \code{TRUE}, the \verb{trend_\{method\}},
\verb{seasonal_\{method\}}, and \verb{remainder_\{method\}} columns are also added (the
full \code{\link[=decompose_series]{decompose_series()}} output).}

\item{params}{Optional list of method-specific parameters for fine control.
Every parameter has a default, so this argument is only needed for
non-standard use cases.

For \strong{STL} (\code{methods = "stl"}):
\itemize{
\item \code{s.window} or \code{stl_s_window}: seasonal smoothing window. Either \code{"periodic"}
(default, assumes constant seasonal pattern) or a positive odd integer
(larger values allow more slowly evolving seasonality).
\item \code{t.window} or \code{stl_t_window}: trend smoothing window (odd integer, or \code{NULL}
to let \code{stats::stl()} choose automatically — recommended default).
\item \code{robust} or \code{stl_robust}: logical. If \code{TRUE}, uses robust fitting to reduce
the influence of outliers. Default \code{FALSE}.
}

For \strong{regression} (\code{methods = "regression"}):
\itemize{
\item \code{poly_raw}: logical. If \code{FALSE} (default), uses orthogonal polynomials
(numerically stable, recommended). If \code{TRUE}, uses raw polynomials
(more interpretable coefficients, less stable for degree >= 2).
}

\strong{classic}, \strong{bsm}, and \strong{seats} take no \code{params}. For multiplicative
seasonality with any method, use \code{transform = "log"}.}

\item{.quiet}{If \code{TRUE}, suppress informational messages.}
}
\value{
A tibble with the original columns plus, for each requested method, a
\verb{seasadj_\{method\}} column holding the seasonally adjusted series. When
\code{components = TRUE}, the \verb{trend_\{method\}}, \verb{seasonal_\{method\}}, and
\verb{remainder_\{method\}} columns are added as well.

The seasonally adjusted series is the series with the seasonal component
removed: \code{trend + remainder} for additive decompositions, \code{trend * remainder} when \code{transform = "log"}. Output rows are ordered by date within
each group; the original row order is not preserved.
}
\description{
Pipe-friendly convenience wrapper around \code{\link[=decompose_series]{decompose_series()}} focused on a
single task: removing the seasonal component from a time series. It adds a
\verb{seasadj_\{method\}} column holding the seasonally adjusted (deseasoned) series
and, optionally, the underlying trend, seasonal, and remainder components.
}
\details{
\code{deseason_series()} is a thin wrapper: it calls \code{\link[=decompose_series]{decompose_series()}} with
\code{seasadj = TRUE} and then keeps only the seasonally adjusted column unless
\code{components = TRUE}. All seasonal-adjustment behaviour, validation, grouping,
and the \code{transform = "log"} (multiplicative) path are inherited unchanged
from \code{\link[=decompose_series]{decompose_series()}}. See its documentation for method internals and the
meaning of the \code{params} argument.

For a full trend/seasonal/remainder decomposition, or for the \code{regression},
\code{classic}, and \code{bsm} methods, use \code{\link[=decompose_series]{decompose_series()}} directly.
}
\examples{
# Seasonally adjust a quarterly series (STL, the default)
gdp_construction |>
  deseason_series(value_col = "index")

# Also keep the trend, seasonal, and remainder components
gdp_construction |>
  deseason_series(value_col = "index", components = TRUE)

# Multiplicative adjustment via log transform (seasonal swings grow with level)
gdp_construction |>
  deseason_series(value_col = "index", transform = "log")

# X-13ARIMA-SEATS adjustment (requires the 'seasonal' package)
if (requireNamespace("seasonal", quietly = TRUE)) {
  gdp_construction |>
    deseason_series(value_col = "index", methods = "seats")
}

# Compare STL and SEATS adjustments side by side
if (requireNamespace("seasonal", quietly = TRUE)) {
  gdp_construction |>
    deseason_series(value_col = "index", methods = c("stl", "seats"))
}

# Grouped seasonal adjustment: one adjustment per electricity sector
electricity |>
  deseason_series(group_cols = "name_series")

}
\seealso{
\code{\link[=decompose_series]{decompose_series()}} for the underlying decomposition and the full
set of methods; \code{\link[=augment_trends]{augment_trends()}} to extract a trend component only.
}
